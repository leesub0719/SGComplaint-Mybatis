<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.transit.SGComplaint.mapper.DdokBusGuideImageMapper">
    <resultMap id="imageMap" type="com.transit.SGComplaint.domain.DdokBusGuideImage">
        <id property="imageNo" column="image_no"/>
        <result property="originalName" column="original_name"/>
        <result property="storedName" column="stored_name"/>
        <result property="filePath" column="file_path"/>
        <result property="contentType" column="content_type"/>
        <result property="fileSize" column="file_size"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findAllOrdered" resultMap="imageMap">
        SELECT image_no, original_name, stored_name, file_path, content_type, file_size, created_at
        FROM sgtransit_ddokbus_guide_image
        ORDER BY image_no ASC
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*)
        FROM sgtransit_ddokbus_guide_image
    </select>
</mapper>
