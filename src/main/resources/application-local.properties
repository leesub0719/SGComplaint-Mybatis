# 로컬 개발용 MySQL 설정
spring.datasource.url=jdbc:mysql://localhost:3306/${DB_NAME:sgcomplaint}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:1q2w3e}

# 로컬에서는 환경변수 미설정 시 실제 문자를 발송할 수 없습니다.
sms.solapi.api-key=${SOLAPI_API_KEY:}
sms.solapi.api-secret=${SOLAPI_API_SECRET:}
sms.solapi.sender=${SOLAPI_SENDER:}

spring.thymeleaf.cache=false
logging.level.com.transit.SGComplaint.mapper=DEBUG
logging.level.com.transit.SGComplaint.repository=DEBUG

# 로컬 업로드 경로
app.upload.complaint-dir=${COMPLAINT_UPLOAD_DIR:uploads/complaints}
app.upload.answer-dir=${ANSWER_UPLOAD_DIR:uploads/answers}
app.upload.notice-image-dir=${NOTICE_IMAGE_UPLOAD_DIR:uploads/notices}
app.upload.main-banner-dir=${MAIN_BANNER_UPLOAD_DIR:uploads/main-banners}
app.upload.ddokbus-guide-dir=${DDOKBUS_GUIDE_DIR:uploads/routes/ddokbus}

server.servlet.session.cookie.secure=false

