# Add these lines to the EXISTING environment file.
# Keep DB_URL, DB_USERNAME, DB_PASSWORD and all existing upload/SMS settings.
# Do not replace the full production environment file with this fragment.

DB_NAME=sgcomplaint
# Enable only after check + baseline (existing DB), or migrate (empty DB).
DB_MIGRATION_ENABLED=true

